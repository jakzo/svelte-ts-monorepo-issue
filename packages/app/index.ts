// Svelte: 'rootDir' is expected to contain all source files.
// Typescript: 'rootDir' is expected to contain all source files.
// import from "../dep";

// Svelte: 'rootDir' is expected to contain all source files.
// Typescript: (no error)
import "dep";
